{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "phonebook-backend",
  "homepage": "https://github.com/fros25/phonebook-backend#readme",
  "bugs": {
    "url": "https://github.com/fros25/phonebook-backend/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fros25/phonebook-backend.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../the-phonebook && npm run build && cp -r dist ../phonebook-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "mongoose": "^9.10.1",
    "morgan": "^1.12.0"
  }
}
